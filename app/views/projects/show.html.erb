<h1><%= @project.name %></h1>
<h1><%= @project.description %></h1>

<p><%= link_to 'Edit', edit_project_path(@project) %></p>
<p><%= link_to 'Delete', project_path(@project), :data => {:confirm => 'You sure?',
                                           :method => 'delete'} %></p>
<p><%= link_to 'Return to Projects', projects_path %></p>
